package com.example.picsearch.ui.theme
import androidx.compose.ui.graphics.Color

// Gallery Black 配色方案 — M3 语义映射
val Primary = Color(0xFF1C1917)          // 主色：深色表面上的强调
val PrimaryVariant = Color(0xFF44403C)   // 次级表面、hover 态
val Secondary = Color(0xFF292524)        // 次要按钮、暗色背景
val Tertiary = Color(0xFF22C55E)         // 成功/进行中状态（绿色）
val TertiaryOn = Color.White

val Background = Color(0xFFF8F6F3)       // 页面背景（暖白）
val Surface = Color(0xFFFFFEFC)          // 卡片、输入框背景
val SurfaceVariant = Color(0xFFF2F0ED)   // 浅灰背景（标签等）
val SurfaceTonal = Color(0xFFE7E5E4)     // tonal elevation 层级

val TextPrimary = Color(0xFF0C0A09)      // 主文本（深色）
val TextSecondary = Color(0xFF6B6560)    // 副文本（中灰）
val TextMuted = Color(0xFFA8A29E)        // 占位符、禁用态（浅灰）
val TextInverse = Color.White            // 深色背景上的反色文本

val BorderColor = Color(0xFFE8E4DF)      // 分割线、边框
val BorderFocus = Color(0xFF78716C)      // 聚焦边框

val ErrorColor = Color(0xFFDC2626)       // 错误态
val ScoreBg = Color(0xCC000000)          // 相似度分数底（半透明黑）
val SceneTagBg = Color(0xE6FFFFFF)       // 场景标签底（半透明白）

// Compose Material3 暗色主题色
val DarkPrimary = Color(0xFFE4E4E7)
val DarkSecondary = Color(0xFFD4D4D8)
val DarkBackground = Color(0xFF09090B)
val DarkSurface = Color(0xFF18181B)
val DarkSurfaceVariant = Color(0xFF27272A)
val DarkOnSurface = Color(0xFFF4F4F5)
val DarkOutline = Color(0xFF3F3F46)
val DarkOutlineVariant = Color(0xFF27272A)
